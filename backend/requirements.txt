fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
rank_bm25==0.2.2
pandas==2.2.3
scipy==1.14.1
pytest==8.3.3
httpx==0.27.2
python-multipart==0.0.9
# Optional, for real generative answers instead of the extractive fallback.
# Uncomment whichever provider you're using (or both):
# google-genai==0.3.0      # Gemini -- free API key, no billing account needed
# openai==1.51.0           # OpenAI -- paid, needs billing set up
